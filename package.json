{
  "name": "assemblyai-mcp",
  "version": "0.1.0",
  "description": "An MCP server for the AssemblyAI API: transcription, subtitles, word search and LeMUR",
  "license": "MIT",
  "type": "module",
  "bin": { "assemblyai-mcp": "dist/index.js" },
  "files": ["dist"],
  "engines": { "node": ">=18" },
  "scripts": {
    "build": "tsc",
    "test": "node --test dist/*.test.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
